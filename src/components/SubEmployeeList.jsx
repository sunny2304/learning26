import React from 'react'

export const SubEmployeeList = (props) => {
  return (
    <div style={{textAlign:"center"}}>
        <h1>
            Sub employee list
            {props.title}
        </h1>
    </div>
  )
}
